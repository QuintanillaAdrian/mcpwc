# Bearer token que debe enviar tu backend en cada request
# Authorization: Bearer <API_TOKEN>
API_TOKEN=change-this-to-a-secure-random-token

# Puerto del servidor Express (opcional, default: 3001)
API_PORT=3001

# Clave AES-256-GCM para descifrar credenciales (hex de 64 chars = 32 bytes)
# Debe coincidir exactamente con la clave que usa el backend para cifrar
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=change-this-to-a-64-char-hex-key

# Tablas de DynamoDB de donde se leen las credenciales de WooCommerce en
# cada /mcp/execute (ver tenantCredentials.ts) — mismos nombres que usa
# infra-stack.ts del backend. Requiere credenciales de AWS con permiso de
# lectura sobre estas dos tablas (aws configure).
DYNAMO_TABLE_TENANTS=tenants
DYNAMO_TABLE_TENANT_WOOCOMMERCE=tenant_woocommerce
AWS_REGION=us-east-2
